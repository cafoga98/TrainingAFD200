import 'package:flutter/material.dart';

class Top10Carrusel extends StatelessWidget {
  const Top10Carrusel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
